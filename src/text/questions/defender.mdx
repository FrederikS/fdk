---
question: What is a defender Method?
---

Defender Methods are `default` methods, which were added in Java 8 to
interfaces. With them, it is possible to add new methods to interfaces without
breaking existing implementations by defining a `default` behavior for all of
them.
